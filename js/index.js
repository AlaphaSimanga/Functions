/*function addNumbers(x,y){
    return x+y;
}
console.log(alert.toString());*/

const pythag = function(width, height){
    let hype = Math.sqrt(width * width + height * height);
    return hype;
  }
   console.log(pythag(2,6));
